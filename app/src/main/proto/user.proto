syntax = "proto3";

option java_package = "com.rifqipadisiliwangi.protodatastore";
option java_multiple_files = true;

message UserProto {
  // filter for showing / hiding completed tasks
  string email = 1;
  string username = 2;
  string password = 3;
  string session = 4;
}